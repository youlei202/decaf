experiment: attribution
profile: boundary-smoke
max_workers: 1
execution:
  backend: gpu
  adapter: decaf.experiments.attribution.gpu_runtime:evaluate_member
  requires_gpu: true
  scheduler: single_gpu_dynamic_queue
  precision: fp32
  fixed_images: 8
  dataset_root_env: DECAF_DATA_ROOT
  checkpoint_root_env: DECAF_CACHE_ROOT
  dataset_manifests:
    partimagenet: ${DECAF_PARTIMAGENET_MANIFEST}
  checkpoint_files:
    idsds_resnet50: ${DECAF_CHECKPOINT_IDSDS_RESNET50}
    torchvision_resnet50: ${DECAF_CHECKPOINT_TORCHVISION_RESNET50}
statistics:
  bootstrap_replicates: 200
  bootstrap_seed: 8218
  endpoint_anchor: decaf_5
plan:
  include:
    - smoke_partimagenet_deletion_targets
    - smoke_partimagenet_heldout_targets
    - smoke_partimagenet_boundary
